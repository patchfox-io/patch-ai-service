# tl'dr what is PatchFox?

PatchFox is genAI for dependency management.

Under the covers, PatchFox is has two parts. 

* A data pipeline that eats time-series data relating to software dependency management, enriches it, and provides an API 

* A chat agent called "Patch" that's installed as a Slack App. 